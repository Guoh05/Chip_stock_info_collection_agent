# Webapp-specific dependencies (Phase 2)
# Pipeline deps live in ../requirements.txt and are shared via the same .venv.

fastapi==0.115.5
uvicorn[standard]==0.32.1
jinja2==3.1.4
python-multipart==0.0.17        # form / file upload parsing
python-dotenv==1.0.1            # load .env (decision #42)
email-validator==2.2.0          # validate user emails

# Pipeline output parsing (already in pipeline requirements but listed for clarity)
openpyxl>=3.1.5

# Cross-platform process-tree kill for the "中断查询" feature
psutil>=5.9
